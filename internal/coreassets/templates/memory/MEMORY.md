# Project memory

Durable, non-obvious knowledge about *this* repo — debugging patterns, conventions,
the reason a workaround exists — that an agent should carry across sessions. This
index is loaded every session; the topic files it lists are read on demand.

What belongs here: working knowledge that helps you work in this repo but is **not**
required behavior (that's the spec library) and isn't already obvious from the code,
git history, or specs. When unsure, see the `managing-memory` skill.

## Topics

<!-- One line per topic file as you create them, e.g.:
- [API conventions](api-conventions.md) — REST envelope {data, error}; errors carry a `code`
- [CI flakes](ci-flakes.md) — the e2e suite needs --no-sandbox in CI -->

_(empty — add topics as you learn them)_
