{
  "stack": "swift-package",
  "family": "swift",
  "memberDir": "packages",
  "nameRule": "identifier",
  "scripts": [
    {
      "phase": 0,
      "commands": [
        "mise trust"
      ]
    },
    {
      "phase": 1,
      "silent": true,
      "commands": [
        "for d in Sources Tests; do if [ -d \"$d\" ]; then swift format --in-place --recursive \"$d\"; fi; done"
      ]
    }
  ],
  "target": {
    "command": "mise //{{.Dir}}:test",
    "format": "swift",
    "report": "{{.Dir}}/test.swift-events.ndjson",
    "source": "{{.Dir}}",
    "bindings": "scoped"
  }
}
