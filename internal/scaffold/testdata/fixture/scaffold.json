{
  "stack": "fixture",
  "scripts": [{ "phase": 0, "commands": ["echo installed"] }],
  "target": {
    "command": "mise run -C {{.Dir}} test",
    "format": "junit",
    "report": "{{.Dir}}/junit.xml",
    "source": "{{.Dir}}/app"
  }
}
