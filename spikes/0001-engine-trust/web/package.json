{
  "name": "spike-web",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=junit --outputFile=report.junit.xml"
  },
  "devDependencies": {
    "vitest": "latest"
  }
}
