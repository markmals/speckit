# The repo root .gitignore ignores all .speckit/ (engine-owned runtime). This
# fixture's specs.json is committed source, so re-include the dir here and
# re-exclude only the generated lock.
!.speckit/
.speckit/lock/

# verify/test side effects — regenerated by the gate, never committed
app/node_modules/
app/junit.xml
